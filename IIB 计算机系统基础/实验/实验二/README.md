This is bomb 201614500.

It belongs to 201614500 (dqwang@hust.edu.cn)
```c
/***************************************************************************
 * 邪恶博士的阴险炸弹，版本 1.1
 * 版权所有 2011，邪恶博士公司。保留所有权利。
 *
 * 许可证：
 *
 * 邪恶博士公司（以下简称“肇事者”）特此授予您（以下简称“受害者”）明确许可使用此炸弹（以下简称“炸弹”）。
 * 这是一个有时间限制的许可证，将在受害者死亡时失效。
 * 肇事者对受害者造成的损害、沮丧、精神错乱、眼睛突出、腕管综合症、失眠或其他伤害不承担任何责任。
 * 除非肇事者想要承担责任，那就是另一回事了。
 * 受害者不得将此炸弹源代码分发给肇事者的任何敌人。
 * 任何受害者不得调试、逆向工程、运行“strings”命令、反编译、解密或使用任何其他技术来了解并拆除炸弹。
 * 处理此程序时不得穿着防爆服装。
 * 肇事者不会为肇事者糟糕的幽默感道歉。
 * 在法律禁止使用炸弹的地方，此许可证无效。
 ***************************************************************************/

#include <stdio.h>
#include <stdlib.h>
#include "support.h"
#include "phases.h"

/* 
 * 提醒自己：记得删除这个文件，这样我的受害者们就不会知道发生了什么，
 * 他们都会在一场壮观的邪恶爆炸中灰飞烟灭。 -- 邪恶博士
 */

FILE *infile;

int main(int argc, char *argv[])
{
    char *input;

    /* 提醒自己：记得将此炸弹移植到 Windows 并为其添加一个华丽的 GUI。 */

    /* 当没有参数运行时，炸弹从标准输入读取输入行。 */
    if (argc == 1) {  
	infile = stdin;
    } 

    /* 当使用一个参数 <file> 运行时，炸弹从 <file> 读取直到 EOF，
     * 然后切换到标准输入。因此，当您拆除每个阶段时，
     * 可以将其拆除字符串添加到 <file> 中，避免重新输入。 */
    else if (argc == 2) {
	if (!(infile = fopen(argv[1], "r"))) {
	    printf("%s: 错误：无法打开 %s\n", argv[0], argv[1]);
	    exit(8);
	}
    }

    /* 您不能使用超过 1 个命令行参数调用炸弹。 */
    else {
	printf("用法：%s [<输入文件>]\n", argv[0]);
	exit(8);
    }

    /* 执行各种秘密操作，使炸弹更难拆除。 */
    initialize_bomb();

    printf("欢迎来到我的邪恶小炸弹。您有 6 个阶段可以用来炸飞自己。祝您愉快！\n");

    /* 嗯... 六个阶段肯定比一个阶段更安全！ */
    input = read_line();             /* 获取输入                   */
    phase_1(input);                  /* 运行阶段                   */
    phase_defused();                 /* 糟糕！他们破解了！
				      * 让我知道他们是怎么做到的。 */
    printf("阶段 1 已拆除。下一个呢？\n");

    /* 第二阶段更难。没人能破解这个... */
    input = read_line();
    phase_2(input);
    phase_defused();
    printf("这是第二个。继续！\n");

    /* 我想这到目前为止太简单了。一些更复杂的代码会让人困惑。 */
    input = read_line();
    phase_3(input);
    phase_defused();
    printf("已经过半了！\n");

    /* 哦，是吗？那么，您的数学如何？试试这个棘手的问题！ */
    input = read_line();
    phase_4(input);
    phase_defused();
    printf("您破解了那个。试试这个。\n");
    
    /* 在内存中转来转去，我们停在哪里，炸弹就在哪里爆炸！ */
    input = read_line();
    phase_5(input);
    phase_defused();
    printf("干得好！继续下一个...\n");

    /* 这个阶段永远不会被使用，因为没人能通过前面的阶段。
     * 但以防万一，把这个阶段做得特别难。 */
    input = read_line();
    phase_6(input);
    phase_defused();

    /* 哇，他们破解了！但是不是少了点什么... 也许他们忽略了什么？
     * 哈哈哈！ */
    
    return 0;
}
```
